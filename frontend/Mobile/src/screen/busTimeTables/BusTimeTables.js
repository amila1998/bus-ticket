import React from 'react'
import { SafeAreaView, ScrollView } from 'react-native'

const BusTimeTables = () => {
  return (
 <SafeAreaView>
    <ScrollView>
        
    </ScrollView>
 </SafeAreaView>
  )
}

export default BusTimeTables